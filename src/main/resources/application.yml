spring:
  application:
    name: SpringGraphQl
  datasource:
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/postgres
  jpa:
    show-sql: true
  graphql:
    graphiql:
      enabled: true